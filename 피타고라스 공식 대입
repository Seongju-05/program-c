#pragma warning (disable : 4996)
#include <stdio.h>
#include <math.h>

struct Point
{
	int x, y;
};

int main()
{
	struct Point p1;
	struct Point p2;
	double a, b, c;
	a = p2.x - p1.x;
	b = p2.y - p1.y;
	c = sqrt(a * a + b * b);
	printf("%.2f\n", c);
return 0;
}
