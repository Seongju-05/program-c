#pragma warning (disable : 4996)
#include <stdio.h>

int main()
{
	int a[5]; 
	int i, max = 0;
	for (i = 0; i < 5; i++)
	{
	printf("%d번 학생의 프c 성적 : ", i + 1);
	scanf("%d", &a[i]);
	}
	for (i = 0; i < 5; i++)
		if (max < a[i])
			max = a[i];
	printf("최고 점수 : %d\n", max);
		return 0;
}
