1.
1을 입력하면 ONE으로 가서 1입니다 출력하기
2을 입력하면 TWO으로 가서 2입니다 출력하기
다른 숫자를 입력하면 그 외 숫자입니다. 출력하기

#pragma warning (disable : 4996)
#include <stdio.h>

int main()
{
	int n;
	printf("자연수 입력 : ");
	scnaf("%d", &n);
	if (n == 1)
		goto ONE;
	else if (n == 2)
		goto TWO;
	else
		goto OTHER;
	label
ONE :
	printf("1입니다.\n");
	goto END;
TWO :
	printf("2입니다.\n");
	goto END;
OTHER:
	printf("그 외 숫자입니다.\n");
END :
	return 0;
}


2.
M,m,A,a,E,e를 입력하면
M, m = Good Morning~~ 출력하기
A, a = Good Afternoon~~ 출력하기
E, e = Good Evening~~ 출력하기

#pragma warning (disable : 4996)
#include <stdio.h>

int main()
{
	char day;
	printf("M : Morning, A : Afternoon, E : Evening\n");
	printf("M,m,A,a,E,e 중에 입력 : ");
	while (1)
	{
		scanf(" %c", &day);
		if (day == 'x' || day == 'X')
			return 0;
		switch (day)// 한글 처리 가능
		{
		case 'M':
		case 'm':
			printf("Good Morning~~\n");
			break;
		case 'A':
		case 'a':
			printf("Good Afternoon~~\n");
			break;
		case 'E':
		case 'e':
			printf("Good Evening~~\n");
		default:
			printf("다시 입력해주세요.");
		}
	}
		return 0;
}
