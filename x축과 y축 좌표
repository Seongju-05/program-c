# pragma warning (disable : 4996)
#include <stdio.h>
#include <math.h>

struct Point
{
	int x, y;
};

int main()
{
	struct Point p;
	p.x = 1;
	p.y = 2;
	printf("X 좌표 : %d, Y 좌표: %d\n", p.x, p.y);
	p.x = 3;
	p.y = 4;
	printf("X 좌표 : %d, Y 좌표: %d\n", p.x, p.y);
}
