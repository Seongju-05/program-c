#pragma warning (disable : 4996)
#include <stdio.h>

int main()
{
	int n1 = 100;
	int* p;
	p = &n1;
	printf("주소 (Address) :%p, %p\n", p, &n1);
	printf("내용, 값 : %d, %d\n", *p, n1);
	return 0;
}
// 주소(Address) :00AFF704, 00AFF704
// 내용, 값 : 100, 100
